# Streaming layer (Phase 1)
confluent-kafka==2.5.3
websockets==13.1
python-dotenv==1.0.1

# Phase 2 — Kafka -> BigQuery consumer
google-cloud-bigquery==3.27.0
# Pin avoids google-auth 2.5x's Regional Access Boundary lookup, which logs a
# noisy "Precondition check failed" warning on every SA refresh (fails open, so
# it's harmless — but the pin keeps logs clean). 2.38.0 is compatible with bq 3.27.
google-auth==2.38.0

# Phase 3 — dbt marts (VWAP / realized vol / trade imbalance)
dbt-bigquery==1.8.2

# Phase 4 — Dash dashboard (deployed on Render)
dash==2.18.2
plotly==5.24.1
pandas==2.2.3
db-dtypes==1.3.0          # BigQuery -> pandas dataframe support
gunicorn==23.0.0          # WSGI server for Render
# dash==2.18.2
# plotly==5.24.1
